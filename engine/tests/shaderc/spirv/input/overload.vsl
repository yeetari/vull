fn overloaded(let value: float): float {
    return 1.0f;
}

fn overloaded(let value: vec3): float {
    return 2.0f;
}

fn vertex_main() {
    let foo = overloaded(1.0f);
    let bar = overloaded(vec3(1.0f));
}
